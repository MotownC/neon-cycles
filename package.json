{
  "name": "neon-light-cycles",
  "version": "0.14.0",
  "private": true,
  "description": "Neon light-cycle game (browser, no build).",
  "scripts": {
    "test": "node --test",
    "start": "node server/server.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
