{
  "name": "neon-light-cycles",
  "version": "0.17.7",
  "private": true,
  "description": "Neon light-cycle game (browser, no build).",
  "scripts": {
    "test": "node --test",
    "start": "node server/server.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
